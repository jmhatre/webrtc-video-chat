# webrtc-video-chat
Browser-to-browser audio and video communication app built with WebRTC. No server relays for media — peer connections stream directly between browsers using the WebRTC API. Includes a lightweight Node.js signaling server for session negotiation. No plugins or installs required.
